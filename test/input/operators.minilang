var x: int := 20;

func a() : int {
    if(x == 15) {
        print(1);
    } else {
        if(x <= 2) {
            print(2);
        }
    }
}

func b(i: int): int {
    while(x >= 0) {
        x := x - 1;
        print(x);
    }
    if(x != 0) {
        print(10);
    }
    return a();
}


func c(j: float) : bool {
    while (x < 16) {
        print(x);
        x := x * 2;
    }

    while (x > 1) {
        print(x);
        x := x / 2;
    }
    return true;
}
